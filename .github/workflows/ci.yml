name: CI PIPELINE

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: Check out code

      - name: Build Docker Image
        run: docker build -t cindy-jewelry .

      - name: Push Docker Image
        run: docker push cindy-jewelry
